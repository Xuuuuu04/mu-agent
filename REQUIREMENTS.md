# Shion 需求规格

## 1. 产品定义

Shion 是面向单一所有者的自托管 A 股投研顾问。核心价值是：

1. 记住长期事实和历史决策。
2. 把自然语言要求落实为工具操作和可跟进任务。
3. 在模型、网络、进程或渠道不稳定时保持可恢复。
4. 对高权限操作保持诚实、可审计和用户最终控制。
5. 将分散市场数据沉淀为可复核的投资假设、证据链和决策记录。

不再要求维持伴侣人格、无目的自主活动或情绪驱动主动联系。历史数据可以保留，但不得改变当前专业行为。

## 2. 功能需求

### F1 对话与渠道

- 支持 QQ、微信、CLI 和 Web。
- 同发送者短时间连发应合并处理。
- 被动回复超过同步窗口后必须转可靠主动投递。
- 非所有者平台消息必须被 bridge 拒绝。

### F2 Agent Core

- 一次 cycle 支持多轮模型与工具调用。
- 工具失败应作为结果回灌，不得直接破坏会话。
- 会话中的 tool use/result 必须配对。
- provider 与工具调用支持取消和时间预算。
- 无最终正文时不得静默伪装成成功。

### F3 记忆

- 保存用户事实、事件、偏好、承诺、会话和任务。
- 支持近期、关键词、时间、实体和可选向量检索。
- 中文关键词必须有不依赖分词质量的兜底。
- 相对日期写入长期状态前必须绝对化。
- 关键状态必须原子落盘并能从损坏输入安全降级。

### F4 Task

- 多步骤事务使用 Task，而不是仅靠会话历史。
- Task 包含 DoD、步骤、下一步、进展、产出和 review。
- 状态迁移必须由代码校验。
- 自主续跑必须有活跃数、wake 数、失败次数和退避上限。
- 完成、阻塞或重要里程碑才能主动通知用户。

### F5 调度

- 允许多个提醒和 Task wake 并存。
- reminder 按用户指定时间执行，不做睡眠 clamp。
- 普通消息不得取消 reminder 或 task。
- 仅 `rest` wake 可被普通消息打断。
- 调度队列必须持久化并兼容旧单 wake 文件。
- 无待办时 cron 不得仅为“保持活跃”调用 LLM。

### F6 子代理

- worker 与 reviewer 使用隔离上下文。
- 子代理不可发送消息、设置提醒或递归 spawn。
- 必须限制并发、总数、turn、token、深度和超时。
- timeout 必须中止 provider，并阻止迟到工具副作用。
- 子代理失败后主代理可以接手，不得触发主会话自愈。

### F7 工具

- 内置工具不可被动态工具覆盖。
- 只有显式标记为纯读取的工具可同轮并行。
- Web 请求必须防 DNS rebinding 和重定向 SSRF。
- Shell 有副作用命令必须由用户以一次性批准号确认。
- 热加载和 MCP 不得绕过上述安全边界。

### F8 Provider

- 支持 Anthropic/OpenAI 协议。
- 支持 primary/fallback 和 provider 冷却。
- 清理不兼容 schema 与非法历史消息。
- 推理模型简单任务可禁用 thinking。
- 空内容、截断和取消必须有明确语义。

### F9 Web

- 只绑定回环，远程访问使用 SSH 隧道。
- 浏览器接口执行同源检查。
- HTTP body 有硬上限。
- 工作台呈现 Shion 的专业定位，不展示伴侣人格叙事。

### F10 A 股数据与研究

- 支持行情、K 线、财务、研报、公告、资金、事件、情绪与 ETF 期权等研究数据。
- 数据结论必须携带可识别的来源和时点；实时数据与历史数据不可混写为同一时点。
- 运行时 MCP 名称变化时只能做唯一后缀解析；存在歧义必须拒绝猜测。
- 交易日历必须有权威来源和有效期，过期或损坏时安全降级。

### F11 Investment Case 与决策日志

- 每个研究标的可保存核心假设、置信度、失效条件、复核日期和证据链。
- 更新 case 不得覆盖既有证据；关键动作以追加式决策日志记录。
- 上下文只注入有限数量的活跃 case，避免历史状态挤占模型窗口。
- 损坏的研究状态必须 fail-closed，不得自动覆盖原文件。

### F12 组合风险与行情看门狗

- 持仓行情快照必须持久化状态、时间、缺失标的与错误原因。
- 组合风险至少覆盖仓位权重、集中度、止损距离、数据缺失和陈旧数据。
- 行情缺失、非法或过期时不得输出伪精确风险数字。
- 金融建议轮次必须禁用通用模型 fallback，并对外暴露真实 provider 健康状态。

### F13 回测与模拟复盘

- 回测信号在收盘形成，最早在下一根 K 线开盘执行，禁止同 bar 前视。
- 模拟 A 股 T+1、100 股一手、佣金最低收费、卖出印花税、滑点、停牌和涨跌停限制。
- 涨跌停规则按板块区分；ST、上市初期等无法由代码确定的状态必须显式输入，不得猜测。
- 行情缓存必须记录数据源、区间、复权方式和复权日期，拒绝不匹配的复权缓存。
- 模拟账户输入使用严格的标准化 JSON 契约，拒绝 NaN/Infinity、非法时间、重复成交 ID；数量和价格单位不明确时拒绝分析。
- 系统不接券商实盘 API，不代客下单，不承诺收益。

## 3. 非功能需求

### 正确性

- 提醒误差不得由内部 clamp 主动扩大。
- 状态机和持久化行为必须有单测。
- 关键跨模块不变量必须以集成测试覆盖。

### 安全

- 默认 fail-closed；不安全兼容模式必须显式开启。
- 模型不能自行批准自己的高权限请求。
- 任意外部内容都视为不可信输入。

### 可靠性

- SQLite 使用 WAL。
- JSON 状态采用原子替换。
- 会话、wake 和 outbox 重启后可恢复。
- 连续失败要自动清理损坏会话并告警。
- Web 金融聚合接口中单项状态损坏不得拖垮其他模块。
- 部署版本必须能从状态接口追溯到 Git revision 和构建时间。
- 部署包只能包含该 Git revision 已跟踪的文件，且不得无条件覆盖运行态交易日历。

### 性能

- 消息 cycle 串行，避免共享会话竞争。
- 只读工具和明确独立子任务可以并行。
- 个人数据规模下检索与状态写入应保持秒级以内。

### 可维护性

- TypeScript strict。
- typecheck、lint、coverage、TS/Python tests 进入 CI。
- 生产兼容名称与产品身份在文档中明确区分。

## 4. 验收

```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm test:py
pnpm test:coverage
```

涉及真实模型、渠道或人格行为的改动，部署后再运行 Shion 回归脚本。
